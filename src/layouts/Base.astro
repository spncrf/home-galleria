---
import settings from "../config/site-settings.js";
import { SkipLink } from "@components/odyssey-theme.js";
import BaseHead from "../components/head/BaseHead.astro";
import type { BaseHeadProps as BaseHeadProps } from "../components/head/BaseHead.astro";

export interface Props {
  seo?: BaseHeadProps;
}

const { seo } = Astro.props;
---

<html lang="en" data-theme=`${settings.settings}`>
  <head>
    <BaseHead {...seo} />
  </head>
  <body>
    <SkipLink />
    <slot name="announcement-bar" />
    <slot name="header" />
    <main id="content">
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>