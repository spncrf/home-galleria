---
import type { BaseHeadProps } from '../components/head/BaseHead.astro';
import Page from './Page.astro';

export interface ArtworkProps {
  seo?: BaseHeadProps;
}

const { seo } = Astro.props as ArtworkProps
---

<Page {seo}>
  <slot />
</Page>